node_modules
.next
.git
.gitignore

docs
*.md
!README.md

# The dashboard image needs neither. detector-sim builds from its own context,
# and the seed script runs on the host against a running dashboard.
services
scripts

.env*.local

playwright-report
test-results
coverage

.qodo
.claude
Dockerfile
.dockerignore
compose.dev.yml
docker-compose.yml
