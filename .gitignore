node_modules/
.next/
out/
build/
next-env.d.ts

.env*.local

playwright-report/
test-results/
coverage/

.DS_Store
*.pem
npm-debug.log*
pnpm-debug.log*

.vercel
*.tsbuildinfo
lh*.json

# pnpm's content-addressable store, created when the visual suite installs
# inside the Playwright container against the bind-mounted repo.
.pnpm-store/

# The footage working directory: the 482MB source clip and the master segment
# cut from it. scripts/prepare-footage.sh downloads the source on demand and
# writes its committed derivatives to public/footage instead.
.footage/
